server:
  port: ${PORT:8082}

logging:
  level:
    org.atmosphere: warn
    root: INFO

spring:
  mustache:
    check-template-location: false
  datasource:
    url: jdbc:postgresql://34.30.8.168:5432/anki?currentSchema=dictionary
    username: postgres
    password: postgres1992
  liquibase:
    change-log: classpath:liquibase/db.changelog-master.yml
    enabled: false

vaadin:
  launch-browser: false
  whitelisted-packages: com.vaadin,org.vaadin,dev.hilla,com.example.application
  productionMode: true
